#!/usr/bin/env bash
set -euo pipefail

script_path="$(readlink -f "$0")"
repo_root="$(cd "$(dirname "${script_path}")/.." && pwd)"
exec python "${repo_root}/tools/run_openclaw_capproof_foreground.py" "$@"
